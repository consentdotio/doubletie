import { log } from './lib/logger';

export async function register() {
	// if (process.env.NEXT_RUNTIME === 'nodejs') {
	// 	const redirectNextjsLogger =
	// 		await require('@doubletie/logger/redirect-nextjs');
	// 	redirectNextjsLogger(log);
	// }

	// const redirectConsoleMethods =
	// 	await require('@doubletie/logger/redirect-console');
	// redirectConsoleMethods(log);
}
